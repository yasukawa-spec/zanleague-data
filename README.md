# ザン企業リーグ データサイト

三人麻雀競技団体「ザンリーグ」企業リーグの配信映像から読み取った成績データ。

- 正式オープン前の暫定公開。`index.html` の `noindex` と `robots.txt` で検索エンジンには載せない
- 数字は読み取り途中の暫定値。選手名は OCR のため綴りが崩れている
- 更新手順: `C:\zanleague` で `node players.mjs` → `index.html` を差し替えて push
